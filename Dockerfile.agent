FROM ubuntu:24.04

RUN apt-get update && apt-get install -y \
    bash git curl ca-certificates ripgrep \
    python3 python3-pip nodejs npm \
 && rm -rf /var/lib/apt/lists/*

RUN useradd -m -u 1000 agent
USER agent
WORKDIR /workspace
CMD ["bash"]
